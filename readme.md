# cartercash mono repo

[`/server`](/server) all logic for slots live here. communicates with the client via websockets.

[`/frontend`](/frontend) react app that renders the slots and handles user input.
